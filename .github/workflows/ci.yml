name: CI

on:
  push:
    # releaser/release: the release PR's branch, pushed over the deploy key, so its checks run.
    branches: [main, releaser/release]
  pull_request:

jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - uses: j178/prek-action@v3.0.0

  # Full suite, including the Docker example (prek runs it with -short).
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      # Full suite with coverage, including the built binary's runs; fails under 90%.
      - run: .github/scripts/coverage.sh

  # Dogfood: releaser releases itself with artifact mode.
  release:
    needs: [prek, test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      # Pushes go over SSH with the deploy key: the main ruleset lets deploy keys bypass it.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ssh-key: ${{ secrets.RELEASE_DEPLOY_KEY }}
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - id: release
        # A canary per push; stable releases through the release PR.
        run: go run ./cmd -prerelease canary -release-pr -commit action.yml -artifacts 'dist/*' -prepare "$PREPARE"
        env:
          GITHUB_TOKEN: ${{ github.token }}
          # Pin action.yml to the new version and cross-compile the binaries it downloads.
          PREPARE: |
            set -e
            sed -i -E 's/(RELEASER_VERSION: ).*/\1"${version}"/' action.yml
            for p in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64; do
              CGO_ENABLED=0 GOOS=${p%/*} GOARCH=${p#*/} go build -trimpath -ldflags="-s -w" -o "dist/releaser-${p%/*}-${p#*/}" ./cmd
            done
      # Marketplace users pin @v1: move the major tag to the new stable release (not HEAD
      # after a merge commit, and never to a canary).
      - if: steps.release.outputs.released == 'true' && steps.release.outputs.prerelease == 'false'
        env:
          TAG: ${{ steps.release.outputs.tag }}
        run: |
          git tag -f "${TAG%%.*}" "$TAG"
          git push -f origin "${TAG%%.*}"
